# Core dependencies for PosNegDM (developed on torch 2.3.0 / Python 3.10).
# For the exact frozen environment, see environment.yml.
torch==2.3.0
transformers
numpy
pandas
scikit-learn
imbalanced-learn
matplotlib
tqdm
